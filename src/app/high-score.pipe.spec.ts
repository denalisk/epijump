import { HighScorePipe } from './high-score.pipe';

describe('HighScorePipe', () => {
  it('create an instance', () => {
    const pipe = new HighScorePipe();
    expect(pipe).toBeTruthy();
  });
});
